import { AddExamenForm } from "@/components/AddExamenForm"
import { Layout } from "@/components/Layout"

export default function AddExamenPage() {
  return (
    <Layout>
      <AddExamenForm />
    </Layout>
  )
} 